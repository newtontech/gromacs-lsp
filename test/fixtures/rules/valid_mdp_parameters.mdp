; Valid MDP fixture: every parameter below is part of the official
; GROMACS MDP option set, so gromacs.mdp.unknown_parameter must NOT fire.
integrator  = md
nsteps      = 50000
dt          = 0.002
coulombtype = PME
rcoulomb    = 1.0
rvdw        = 1.0
constraints = all-bonds
