; Invalid topology fixture for the gromacs.topology.missing_include rule.
; Line 10 references an included topology file that does not exist next to
; this fixture, so it must raise gromacs.topology.missing_include.
; Line 15 references a file that DOES exist alongside this fixture
; (missing_include_present.itp), so it must NOT raise the rule.

[ defaults ]
1       2       yes     0.5     0.5

#include "does_not_exist.itp"

[ moleculetype ]
SOL     3

#include "missing_include_present.itp"

[ atoms ]
1       P1      1       SOL     O       1       0.000   16.00

[ system ]
Single molecule

[ molecules ]
SOL     1
